import { UsernamefilterPipe } from './usernamefilter.pipe';

describe('UsernamefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UsernamefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
