import { FilterreportsLeadsPipe } from './filterreports-leads.pipe';

describe('FilterreportsLeadsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterreportsLeadsPipe();
    expect(pipe).toBeTruthy();
  });
});
