import { ContactpersonsPipe } from './contactpersons.pipe';

describe('ContactpersonsPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactpersonsPipe();
    expect(pipe).toBeTruthy();
  });
});
