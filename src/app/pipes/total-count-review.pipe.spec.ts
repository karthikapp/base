import { TotalCountReviewPipe } from './total-count-review.pipe';

describe('TotalCountReviewPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalCountReviewPipe();
    expect(pipe).toBeTruthy();
  });
});
