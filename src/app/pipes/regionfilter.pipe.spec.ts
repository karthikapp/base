import { RegionfilterPipe } from './regionfilter.pipe';

describe('RegionfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RegionfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
