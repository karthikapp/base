import { FilterexecPipe } from './filterexec.pipe';

describe('FilterexecPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterexecPipe();
    expect(pipe).toBeTruthy();
  });
});
