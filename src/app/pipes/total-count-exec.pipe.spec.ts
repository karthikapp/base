import { TotalCountExecPipe } from './total-count-exec.pipe';

describe('TotalCountExecPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalCountExecPipe();
    expect(pipe).toBeTruthy();
  });
});
