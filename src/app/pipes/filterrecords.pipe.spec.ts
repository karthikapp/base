import { FilterrecordsPipe } from './filterrecords.pipe';

describe('FilterrecordsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterrecordsPipe();
    expect(pipe).toBeTruthy();
  });
});
