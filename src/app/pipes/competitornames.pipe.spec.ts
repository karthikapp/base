import { CompetitornamesPipe } from './competitornames.pipe';

describe('CompetitornamesPipe', () => {
  it('create an instance', () => {
    const pipe = new CompetitornamesPipe();
    expect(pipe).toBeTruthy();
  });
});
