import { FilterreviewsPipe } from './filterreviews.pipe';

describe('FilterreviewsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterreviewsPipe();
    expect(pipe).toBeTruthy();
  });
});
