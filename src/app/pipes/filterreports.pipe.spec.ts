import { FilterreportsPipe } from './filterreports.pipe';

describe('FilterreportsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterreportsPipe();
    expect(pipe).toBeTruthy();
  });
});
