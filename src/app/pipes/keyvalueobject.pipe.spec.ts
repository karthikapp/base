import { KeyvalueobjectPipe } from './keyvalueobject.pipe';

describe('KeyvalueobjectPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyvalueobjectPipe();
    expect(pipe).toBeTruthy();
  });
});
